export enum Currency {
  EUR = 'EUR',
  USD = 'USD',
}

// Add $
export enum PricePer {
  EUR = 'EUR',
  EUR_M2 = 'EUR/m²',
  EUR_HOURS = 'EUR/h',
}

export enum PriceType {
  GROSS = 'GROSS',
  NET = 'NET',
}
